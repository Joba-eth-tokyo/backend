export enum ProjectStatus {
  awaiting_acceptance = 'awaiting acceptance',
  pending = 'pending',
  in_progress = 'in progress',
  in_review = 'in review',
  complete = 'complete',
  dispute = 'dispute',
  refunded = 'refunded',
  rejected = 'rejected',
}
